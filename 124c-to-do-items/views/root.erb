<% @todos.each do |tasks| %>
	<% if tasks.is_completed == false %>
       <input type="checkbox"> <%= tasks.description %><br>
    <% else %>
		<input type="checkbox" checked="checked"> <%= tasks.description %> <br>
	<% end %>
<% end %>