<h1>World cities</h1>

<table>
	<% @cities.each do |city| %>
	<tr>
		<td><%= city.city %>, <%= city.country %></td>
		<td>
			<img src="/images/<%= city.flag %>">
		</td>
		<td>
			<a href="/cities/<%= city.id %>">Details</a>
		</td>
	<tr>
	<% end %>
</table>
	
